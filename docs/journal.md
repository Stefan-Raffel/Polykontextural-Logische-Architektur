# Journal

**Eine Zeile je Zug.** Datum, Hash, ein Satz, und — wenn es einen gab — der Fund.
Der ausfuehrliche Bericht steht in der Commit-Nachricht; sie ist an den Stand gebunden,
das Journal ist der Faden dazwischen.

Dieses Journal ersetzt seit `CLAUDE.md` §11 das Ergebnisdokument je Zug. Ein eigenes
Dokument entsteht nur noch auf Verlangen oder wenn ein Fund laenger ist als eine
Commit-Nachricht — eine Erhebung, eine Begutachtung, eine Messung mit Tafel.

Kennzahlen stehen **nicht** hier, sondern in [`kennzahlen.md`](kennzahlen.md).

**Fund** heisst: etwas, das ohne den Zug unbemerkt geblieben waere — ein falscher Wert im
Bestand, eine Route, die zu klein misst, eine Vorgabe, die ihren eigenen Zweck verfehlt.
Kein Fund ist auch eine Auskunft und wird als `—` geschrieben.

---

| Datum | Commit | Zug | Fund |
|---|---|---|---|
| 2026-08-05 | `465c3af` | Ordnungswechsel-Differential: arme Schranke und Pfeil-Aufstieg | die arme Haelfte faellt axiomfrei aus |
| 2026-08-05 | `c6d3887` | Die Klammer: eine Beweisform, zwei Zeugen | die Kontaktzahl bewegt sich nicht — ihr Namensmuster trifft den kategorialen Strang nicht mehr |
| 2026-08-05 | `59b8731` `fccc83a` | Formulierungs-Zug, Ablage und Bestand getrennt | ein Gegenstand lag in Lean, den die Vorgabe der Ablage zugewiesen hatte |
| 2026-08-05 | `6510cab` | Ziffern-Wache: Lint-Gruppe (E) | `rev1`–`rev3` tragen null Ziffern; die Traegertafel ist eine Erfindung der vierten Ausgabe |
| 2026-08-06 | `786d495` | Rev5, Schritt 1: Rev4 archiviert, Muss-Anker gesetzt | die Archivierung bewegt N1 nicht — das Papier nennt den Suchbegriff bewusst nicht |
| 2026-08-06 | `df7c1cc` | Rev5, Nachzug: zehnte Messregel | eine Auflage stuetzte eine Aussage ueber vierzig Traeger auf die Pruefung von dreien |
| 2026-08-06 | `292844f` `0b2bc66` | Rueckbau 1: `kennzahlen.sh`, §13, `docs/kennzahlen.md` | N1 ohne Wortgrenze zaehlt `sorryAx` mit — 230 statt 144 |
| 2026-08-06 | `7f2c48e` | Rueckbau 2: Kennzahlentafel, Journal, kein Befund je Zug | zwoelf Mess-Marken im Papier waren ersetzbar, ohne dass eine Aussage verloren geht |
| 2026-08-06 | `9535c3f` | Profil-Route: `profil_probe.sh`, Profilspalte gemessen statt getippt | 44 Zeilen bestaetigt, 0 Abweichungen — die getippte Spalte war korrekt, und das ist jetzt geprueft statt geglaubt |
| 2026-08-06 | `d9e6e80` `0eedc78` | Manifest der Prozessoptimierung, Befund zum Erreichten | "vier Gleichungen" war nach einem Tag falsch — Zahlwort gestrichen statt nachgefuehrt; die Ziffern-Wache traf den Befund selbst — und `d9e6e80` wurde ROT committet, weil ich den Exit-Code mit einem `echo` verschluckt hatte; `0eedc78` repariert es |
| 2026-08-06 | `6fe7d53` | Ausgabenprobe gebaut; letzte Marke der Traegertafel entfaellt | die vier Groessen der Vorgabe fangen einen gestrichenen Absatz NICHT — eine fuenfte war noetig |
| 2026-08-06 | `bb2ad69` | Ausgabenprobe fuer md gegen html geeicht; Erzeugung Rev5 begonnen | sechs Fehler in der Probe selbst — `<p[^>]*>` faengt auch `<pre>` |
| 2026-08-06 | `d7104ff` | Ankerprobe (F), Traegertafel als ganze Zeile, Zitatbloecke; Streckenprobe gefahren | ein Wandler liess eine SETZUNG fallen und alle Groessen schwiegen — die sechste Groesse ist daraus entstanden |
| 2026-08-06 | `745369b` | Siebte Groesse (Raenge); Streckenprobe an Teil B | Teil B haelt alle sieben — aber die Ausnahmen-Pruefung findet vier Klassen mit Inhalt und ohne Deckung |
| 2026-08-06 | `e4b7102` | Achte Groesse (Code), Bildunterschriften im Wortlaut, Ausnahmen-Pruefung | die Bildunterschrift von Figur 4 (de) und 8 (en) weicht in Rev4 vom Entwurf ab — das SVG ist unveraendert |
| 2026-08-06 | `e696870` | **Die fuenfte Ausgabe, erzeugt** — beide Sprachen, acht Groessen Exit 0 | zwei Bildunterschriften weichen von Rev4 ab, ohne dass sich die Figur bewegt hat |
| 2026-08-06 | `fb0630b` | Neunte Groesse: Anzeigeformeln, Quelle gegen Darstellung getrennt | die rohe LaTeX-Quelle stand sichtbar auf der Seite — der erste blinde Fleck, den ein Leser fand und keine Probe |
| 2026-08-07 | `19f586f` | Verbundkontextur als Struktur: `CompoundContexture`, Ledger Rev. 11 | zwei Profil-Erwartungen falsch — die Finset-Gestalt zieht Choice auf drei unabhaengigen Routen |
| 2026-08-07 | `20d4c0b` | Paarmischung: `PairwiseMixture`, Ledger Rev. 12 | der Choice-Anteil sitzt in der GESTALT DER VORAUSSETZUNG, nicht im Argument — zwei Fassungen, eine Groesse Unterschied |
| 2026-08-07 | `2b8cfa6` | Fallstrick 8 um die `allowOpaque`-Heilung ergaenzt | zwei Instanzen, zwei Routen, dieselben sieben Namen — der Kern des Eintrags bleibt: ohne das Argument ist die Route blind |
| 2026-08-07 | `00d4941` | Kuer des Morphogramm-Zugs: `Kenogram/Fiber`, Ledger Rev. 13 | die Profil-Erwartung war durch LESEN falsifizierbar — die Wache des konsumierten Satzes stand zwei Dateien weiter |
| 2026-08-08 | `e0f7893` | **Die sechste Ausgabe, erzeugt** — acht neue Ziffern, Tafel 40 zu 48 | die Strecke loeschte Marken ohne Figur-Kopf STILLSCHWEIGEND — getroffen hat es den Absatz, der das Markenzeichen erklaert; er stand in beiden Sprachfassungen ohne Satzgegenstand, und keine der neun Groessen sah es, weil die Probe dieselbe Ausnahme spiegelt |
| 2026-08-08 | `86578fc` | Wertblindheit der Kanonisierung: `RelabelInvariance`, Ledger Rev. 14 | die Kontaktbehauptung fiel an der eigenen Messung — S2 hat KEINEN direkten Termkontakt (Keno 0), die Verbindung liegt in der Aussage, und die Kontaktzahl meint kategorial und nicht Proemial |
| 2026-08-09 | `bf7d82f` | Trennsatz auf der Wertschicht: `Kenogram/OccupancySeparation`, Ledger Rev. 16 | Bedingung (iv) der Vorgabe ist unerfuellbar, und der Nachweis ist ein Satz: ueber zwei Stellen und einer blossen linearen Ordnung IST jedes trennende Kriterienpaar das Ordnungspaar — wer trennt, benennt `≤` um; die Struktur-Tafel des README lag dabei 25 Saetze hinter dem Stand und ihre Gleichung fiel |
| 2026-08-09 | `bd570be` | Nachtrag zur Abnahme: `§20` als Quellenparagraph ausgewiesen | die feste 19 in `doc_lint` R5 ist die STATUSLAGE und keine vergessene Obergrenze — der Korpus beansprucht fuer §20 keinen formalen Traeger, und ohne den Halbsatz liest die naechste Instanz die Zahl als Luecke und fuellt sie |
| 2026-08-09 | `53cb3bb` (ausgelieferter Stand) | Repositorium wieder oeffentlich, GitHub Pages aktiv | die Auslieferung ist gemessen und nicht angenommen: `has_pages` true, `private` false, Default `main`, und `kennzahlen.md`, `definition-ledger.md`, `journal.md` sind live byte-gleich mit dem Arbeitsbaum. **Der Fund steckt in der Gegenprobe:** `index.html` stimmt ebenfalls ueberein und belegt trotzdem NICHTS — kein Zug hat sie bewegt. Frisch ist eine Auslieferung nur an einer Datei nachweisbar, die der letzte Zug geaendert hat; sonst prueft man die Erreichbarkeit und nennt es Aktualitaet |
| 2026-08-09 | `238504e` | Stufenschranke der Paare: `Kenogram/PairStageBound`, Ledger Rev. 17 | zwei Funde, beide gegen Vermutungen: das neue Modul braucht die `maxRecDepth`-Option nicht (gemessen in einer eigenen Datei, n=5 unter einer Sekunde) — ob `Basic.lean` sie an ihrem Ort noch braucht, ist NICHT gemessen; und Mathlib FUEHRT die Bell-Zahlen, was die allgemeine Fassung trotzdem nicht billig macht, weil die Bruecke zu `RGS n` fehlt und `Nat.bell` ausserhalb der eigenen Datei nirgends konsumiert ist |
| 2026-08-09 | `238504e` (berichtigte Zeile) | Ruecknahme aus der Abnahme: eine Zusammenfassung ueberschritt ihre Messung | die Zeile darueber sagte „die Marken der Berechnungs-Reihe sind entbehrlich“; gemessen war eine eigene Datei und nicht die Marke an ihrem Ort. Gattung wie „Mahler hat sie“ im K1K6-Befund: nicht der Fund ist falsch, sondern sein Umfang. Die Commit-Nachricht von `238504e` traegt den zu starken Wortlaut weiter — die Geschichte behaelt, was das Dokument verliert |
| 2026-08-09 | `b43b4d4` | Verkettung unter Identifikation: `Kenogram/IdentifiedConcat`, Ledger Rev. 18 | Phase 1 hat die Bauform entschieden: mit beliebigem `g` zerfaellt das Muster der hinteren Reihe in 165 gezaehlten Faellen, unter injektivem in null — darum ist die Identifikation eine EINBETTUNG. Und die Gegenprobe zu Z3 traegt die Zurueckhaltung: 280488 Paare liefern dasselbe Ergebnis bei verschiedener Belegung, davon null mehr als eine Umbenennung — eine Route, die nichts ansieht, haette dieselbe Null geliefert |
| 2026-08-09 | `b43b4d4` (Kopf-Auflage) | Reichweitengrenze im Kopf nachgetragen: der nichtleere Zeuge gilt fuer die IDENTITAET | Z3 gilt fuer jede Identifikation, sein Zeuge benutzt die leere Reihe; der nichtleere Zeuge daneben ist fuer `Embedding.refl` gebaut. Beide Saetze sind einzeln richtig — zusammengelesen stuenden sie fuer mehr, als bewiesen ist. Auflage aus der Abnahme §3, ohne Bau |
| 2026-08-09 | `5e1f30e` | Das zweite Zeugnis: `Proemial/ContextureEscapeBound`, Ledger Rev. 19 | zwei Erwartungen gefallen: das Modul ist DURCHGAENGIG choice-frei (der Kopf hatte die `Substructure`-Maschinerie verdaechtigt), und die verbindliche Satzroute sieht `@[attr] private lemma` NICHT — sie erwartet die Sichtbarkeit vor dem Attribut, Lean stellt das Attribut voran. Im Bestand nullmal, dieses Modul haette die Blindstelle eingefuehrt |
| 2026-08-09 | `bf5aba3` | **Die siebte Ausgabe, erzeugt** — weite Lesart gesetzt, Tafel 48 -> 60 | zwei Zahlen der Planung fielen an eigenen Routen: es sind ZWOELF neue Ledger-Zeilen und nicht neun (der Anker war der falsche), und die Rangaussage der sechsten Ausgabe faellt — drei Traeger stehen bei sieben. Diesmal war sie an ihrem Anker richtig und der BESTAND hat sich bewegt; die Gattung heisst Haltbarkeit und steht jetzt in der Fehlerliste |
| 2026-08-09 | `751b016` | Auflage zur siebten Ausgabe: Fassungsbezeichnung an EINER Stelle | Titel, Kopfleiste und Fusszeile nannten die sechste, waehrend der Untertitel die siebte nannte — in BEIDEN Sprachen, und `parity` schwieg genau deshalb. Neun brechende Groessen, figures und doc_lint liefen gruen: die Fassungsbezeichnung fuehrt KEINE Probe. Geheilt durch Aufhebung der Mehrfachnennung, nicht durch dreimaliges Nachfuehren |
| 2026-08-09 | `63a9b8c` | Nacharbeit zur Abnahme: die Fassungsnavigation als KLASSE | die Auflage war groesser als ihre Liste — der Erzeugungslauf fuehrte den Inhalt nach, die Navigation nicht. Route statt Liste, drei Klassen: fortgeschrieben, ergaenzt (Rev6 fehlte in drei Ketten, und die Ankerzahl war vier fuer fuenf Fassungen), datiert-bleibt (204 Fundstellen im Archiv, vor und nach dem Zug gleich) |
| 2026-08-09 | `98133d1` | Die zehnte Groesse: Fassungswoerter (Moebel, Ketten, Archiv) | die Vorprobe fiel gegen die eigene Vorgabe aus: freie Prosa ist von datierter Prosa am Muster NICHT zu trennen — `Fassung Rev6` in einem laufenden Satz und `mit der deutschen Fassung Rev2 126` in einer Messgeschichte sehen gleich aus. Die Groesse wurde auf Moebel, Ketten und Archiv geschnitten und der Ausschluss in ihren Kopf geschrieben |
| 2026-08-09 | `cf19481` | Rev8-Merkliste 2.1: die sechste Fundstelle der Fassungsnavigation geheilt | keiner; die Stelle stand in freier Prosa und war darum von der zehnten Groesse aus angebbarem Grund nicht zu fangen. Der Befund hatte sie zuerst falsch zitiert — aus dem Gedaechtnis statt am Codepunkt |
| 2026-08-09 | `ec0ff05` | Rev8-Merkliste 4.1: die Restdifferenz der Inline-Auszeichnung geklaert und geheilt | die seit der Rev6-Liste ungeklaerte Restdifferenz war kein Messartefakt, sondern ein Darstellungsschaden: verschachtelte Auszeichnung, die der Wandler nicht aufloest, mit 8 rohen Sternchen in `docs/de.html` und 14 in `docs/en.html` — sichtbar im veroeffentlichten Papier, und keine der zehn Groessen sah es. Drei Klassen geheilt, danach 0 |
| 2026-08-09 | `f502855` | Die elfte Groesse: Fettauszeichnung, Folge statt Menge | die frueheren Anlaeufe scheiterten an der MENGE (73 gegen 62, 59 gegen 58); die Folge stimmt exakt (456 und 446). Im Muss-Fall blieben die uebrigen zehn Groessen gruen — ein Schaden, der nur die Auszeichnung betrifft, laesst den Absatz vollstaendig stehen |
| 2026-08-09 | `9251e5a` | Reichweite der elften Groesse je Schadensklasse gemessen, Kopf berichtigt | die Angabe des Vorzuges war falsch: die elfte Groesse faengt ZWEI von drei Klassen, nicht drei. Klasse 2 (kursiv-in-kursiv) laesst alle elf gruen und stand mit 2 rohen Sternchen in der Ausgabe. Die Behauptung war aus dem Bau abgeleitet statt gemessen; gefunden hat sie der Pruefpunkt der Abnahme, nicht der Bau |
| 2026-08-09 | `38312a6` | Die vierte Grenze: Gegenseitigkeit zurueckgenommen, eine Richtung gesetzt | die Grenze behauptete, Umtausch und Ordnung bedingten EINANDER, waehrend dasselbe Kapitel zwei Seiten weiter sagt „kein Satz misst die Ordnung am Umtausch" — zwei Stellen derselben Ausgabe im Widerspruch. Gattung: die Iff-Form eines Kriteriums als Gegenseitigkeit der Sache gelesen. Fehlerlisten-Eintrag in beiden Sprachen. Nebenfund, nicht geheilt: die Gruppe „Und drei aus dieser Ausgabe" fuehrt seit Rev7 fuenf Punkte |
| 2026-08-09 | `68ea154` | Auflage zur vierten Grenze: das Zahlwort ueber der Fehlerlisten-Gruppe, zahlfrei geheilt | die Gruppe fuehrte seit Rev7 fuenf Punkte unter „Und drei aus dieser Ausgabe". Beim Heilen fiel auf, dass die KOLLEKTIVAUSSAGE mitkippt: „alle sitzen dort, wo das Papier ueber sich selbst spricht" trifft vier von fuenf — ein blosses Streichen des Zahlworts haette eine falsche Allaussage hinterlassen. Ausnahme jetzt beim Inhalt genannt, nicht an der Position |
| 2026-08-09 | `9c66339` | Die halbe Bruecke: `Kenogram/PartitionCount`, Ledger Rev. 20 | kein Fund im Bau — die Vorprobe hatte alles vorweggenommen, und das ist ihr Zweck. Zwei Sachen sind trotzdem erst hier gemessen worden: `#synth` loest im Modulkontext dieselbe Instanz auf wie in der Wegwerfdatei, und alle vier Werte fallen ohne jede Options-Erhoehung. Der Satz nennt die Bell-Zahl NICHT; dass `Nat.bell` die Partitionen zaehlt, ist auch in Mathlib ein offener Posten |
| 2026-08-09 | `9c0c6f1` | Klaerung zur halben Bruecke: die gemiedene Wendung gesetzt | die Vermeidung war gemeldet, ihre Entbehrlichkeit nicht geprueft. Gemessen: die Zeile „Kein `sorry`, kein `axiom`, kein `: True`-Feld" ist Hausform (36 Module, im Kenogram-Zweig 7 von 15, darunter alle vier zuletzt gebauten) — also die treffendere Formulierung, und sie wird gesetzt statt der Kennzahl zuliebe gemieden. N1 153->154, ein Vorkommen in einer Datei. Nebenfund: das README fuehrte 144/101 und war schon vor dem Zug um 9 veraltet; nachgefuehrt auf 154/111 |
| 2026-09-12 | `673ddb8` | Die Grundlinie (Zug 1): Archiv je Verzeichnis, zwoelfte Groesse, dritte Lueckenzahl mit Route, Satzroute geweitet, drei Zahlenstellen auf Zeiger | `doc_lint` war schon am Vorstand rot, und nicht vom Repo: 42 Traegertafel-Ziffern in 13 Korpusdokumenten der Rev8-Beratung vom selben Tag. Die Kennzahlentafel sagt darum „nicht zu verwenden", obwohl jede Gleichung haelt — der Vermerk nennt eine Gleichung, wo der Lint gemeint ist. Und seit dem 10. August fehlten die Journalzeilen: 15 Zuege, und kein Kriterium hat es gemerkt |
| 2026-09-12 | `2878501` | Abnahme zu Zug 1 umgesetzt: Lint-Gruppe (E) prueft das Repo, nicht den Korpus; zwei Klauseln im Skriptkopf und in der Tafel | die Probe war an einem Commit verankert und hing an Dateien, die in keinem Commit stehen — derselbe Commit lieferte an zwei Tagen zwei Tafeln. Nach dem Schnitt Exit 0, und eine eingesetzte Ziffer im Repo bricht weiterhin |
| 2026-09-12 | `402fa65` | **Die achte Ausgabe, erzeugt** — drei Gestalten der Deutung, der Grund der Dreiheit, der Trennsatz in Kapitel 7, das Quellenverzeichnis; Rev7 archiviert | drei vorgegebene Wortlaute hielten der Messung nicht stand, und alle drei in Zahlen oder Zählungen: der Choice-Satz zählte „acht von acht", gemessen sind vierzehn Sätze des Moduls (die Aussage hält, die Zahl nicht); die Seitenangabe „S. 4 (Druck)" ist die Seite der Datei, der Druck zählt anders; und das Quellenverzeichnis sollte acht Titel führen, das Papier zitiert fünf. Dazu ein Fund an der Fehlerliste selbst: seit der siebten Ausgabe führte eine Gruppe drei Einträge der sechsten unter der Überschrift „aus dieser Ausgabe" |
| 2026-09-14 | `7031c9b` | **Günthers Vermittlungsmass gebaut** — `Proemial/SharedPlaceGrowth`, D1–D5 der Spec, der Zielsatz Z über eine Hebung mit Zeugen statt über eine geschlossene Formel, Z' als `m^6`-Schranke; Eichwerte 8/27/64/125 und 4/135/960/4250 eingefroren | drei Funde, zwei davon an Formulierungen des eigenen Kopfes. Die Aussage über das Axiomprofil war über vierzehn Sätze gemessen und über alle behauptet — choice-frei sind **zwei** der neunzehn, nicht einer; und die Ursache liegt gemessen bei `Fin.fintype` selbst und nicht an der Finset-Gestalt über Funktionsräumen, was den Vermerk im Kopf von `CompoundContexture` verschärft. Der dritte Fund ist eine Schranke der Auswertung: D4 naiv entschieden liefert `mediationCount 5` auch nach 27 Minuten nicht, über `decidable_of_iff` und die Kriteriums-Auflösung in rund zwei Sekunden — die Definition bleibt dabei D4. Nebenbefund an zwei fremden Dokumenten, gemeldet und nicht entschieden: die Spec liest Günthers 27 und 64 als Subsystem-Zahlen, der Appendix-Befund (§5 L6) und die N1-Messung lesen „(3, 64)" als seine zwei durchgerechneten Fälle der Relation; auf der zweiten Lesart trifft das Mass die 64 nicht |
| 2026-09-14 | `a448320` | **Abnahme B2 umgesetzt** — der Choice-Vermerk in `CompoundContexture` als Nachtrag verschärft: die Quelle ist der Trägertyp und nicht die Finset-Gestalt über Funktionsräumen | kein Fund im Zug selbst; der Fund stammt aus dem Vermittlungsmass-Bau und wird hier nur an seinen Ort gebracht. Zwei Sachen sind trotzdem erst hier gemessen worden: der Vermerk stand an genau **einer** lebenden Stelle im Repo (die zwei weiteren Treffer sind Journalzeilen, die einen Stand ihres Datums halten), und der Kopf von `ContextureOverlap`, auf den er sich beruft, ist **nicht** betroffen — sein Satz meidet die Finset-Entscheidung und bleibt richtig. Keine Kennzahl bewegt sich: reine Prosa, alle Werte stehen auf dem Stand von `7031c9b` |
| 2026-09-14 | `c8149da` | **Befund 5 nachgeführt** — der Kopf von `SharedPlaceGrowth` führte den Fund noch als „gemeldet und nicht entschieden", während die Begutachtung ihn entschieden hatte | §12 Regel 7 in eigener Sache: der eigene Modulkopf war still falsch geworden, und kein Bau bricht daran. An der Klärung nachgeprüft statt weitergereicht — K1/K3/K4 trennen die zwei Zahlen („nur dreimal" gilt den variablen-fixierten überbalancierten Subsystemen der Tafel XII, die 64 der kombinierten Art und dort als **obere Schranke, keine Zählung**). Nebenfund, der bisher fehlte und A1 betrifft: die Formel `m³` ist nach K2 **erschlossen** und ausdrücklich nicht quellenfest — quellenfest sind Günthers zwei Zahlen, nicht ihr Bildungsgesetz. Keine Kennzahl bewegt sich |
| 2026-09-14 | `e95a5ea` | **Nachtrag Vermittlungsmass** — die Anwendung ist Setzung und nicht Lesart: der Lille-Appendix gibt drei weitere Kandidaten (verschiedene fixierte Variable, beides zugleich, gemeinsame Diagonalstelle); F1/F2 in den Kopf | der vorgeschlagene Wortlaut „`mediation_strictly_monotone` gilt für jeden von ihnen" sagte mehr als gemessen — bewiesen ist der Satz für den gebauten Kandidaten, für die drei übrigen ist die Monotonie für `m = 2..5` gerechnet (alle sechzehn Zahlen unabhängig nachgerechnet und gleich). Die Hebung trüge zwei der drei mit denselben Bauteilen, die Diagonalstelle nicht — `extend` hebt sie nicht auf eine Diagonalstelle. Und ein Fund in eigener Sache: der Aggregat-Eintrag in `Proemial.lean` führte seit `7031c9b` noch „Choice durchgängig ausser `lift_injective`" — im Modulkopf berichtigt, dort nicht; choice-frei sind zwei der neunzehn |
| 2026-09-16 | `7506857` | **Unmöglichkeitssatz C2 gebaut** — `Proemial/TournamentInseparability`: erhält eine Relation beliebiger Stelligkeit die sechs transitiven lokal-klassischen Operationen auf `Fin 3`, so alle acht; zwei Gleichungen in Tiefe eins und eine Einsetzung, kein Klon-Begriff; Turniertafel eingefroren, Gegenprobe P4 als Satz | die Definition konnte nicht über E1 laufen: dieselbe Gleichung über `ofChoices` trägt `[propext]`, über der Bool-Fallunterscheidung ist sie axiomfrei — die Gleichheit beider Definitionen ist nicht bewiesen. Die vier Erhalter der P4-Relation sind genau die vier in E1 erzeugbaren Vektoren; außerhalb gerechnet ist die Relation eine Invariante des E1-Klons, im Korpus nicht verbunden. Die Kennung C2 ist doppelt belegt (Buildabdeckungs-Zug). In eigener Sache: die erste Zusammenfassung der vier Erhalter war falsch und schloss DDK ein; `decide` hat sie widerlegt |
| 2026-09-16 | `e5b31c8` | **C2 steht bei Günther** — Modulkopf und Aggregat-Zeile von `TournamentInseparability` berichtigt: `kkd_eq` ist eine Formel von C&V S. 28, `transitive_iff` inhaltsgleich mit Günthers Kriterium, der Zielsatz steht nicht bei ihm; die Konvention mit Wertzuordnung; Lesart bleibt das Wort „Turnier" | die Buchstaben-Übersetzung hängt an der Wertzuordnung: unter `x ↦ 3 − x` bleibt K Konjunktion und `kkd_eq` ist Günthers erste Formel mit denselben Buchstaben, unter `x ↦ x + 1` tauschen K und D — der eigene Befund „unser KKD ist sein DDK" hatte stillschweigend die zweite Zuordnung angenommen. Der Zielsatz selbst steht nicht bei Günther; eine Streichung von „nichts über Günther" hätte ihn mit hineingenommen. An der Vorgabe ein Zitat berichtigt („The arrows", nicht „the arrows"). Keine Kennzahl bewegt sich |
| 2026-09-16 | `2c075aa` | **Q4a gebaut** — `Proemial/TournamentSplitThree`: die Turniere auf `Fin 3` sind punktweise die Turniere der acht lokal-klassischen Operationen; darauf acht, sechs transitiv, zwei zyklisch in Bijektions- und Iff-Form, ohne `Fintype.card`; Günthers Kriterium auf Turnieren aus `transitive_iff` transportiert | die Choice-Warnung der Spec (A5) trifft den Fall n = 3 nicht: sie hängt an `Fintype.card` über `Fin`, und die Bijektionsform umgeht es — kein Satz des Moduls trägt `Classical.choice`. `[propext, Quot.sound]` kommt aus der Handzerlegung von `Fin 3`, nicht aus der Zählung. An der Spec, nicht nachgeführt: §0 schliesst Aussagen für n ≠ 3 aus, die A3 verlangt, und A6 führt noch die alte, weitere Lesart |
| 2026-09-16 | `5896151` | **C2 für alle m gebaut** — `Proemial/LocalOpInseparability`: erhält eine Relation beliebiger Stelligkeit über `Fin m` `min`, `max` und alle Verschiebungen, so jede lokal-klassische Operation; Korollar: keine Relation trennt die zyklischen von den transitiven, für jedes `m`; A8: für `m ≥ 4` bricht eine Verschiebung die Invariante der E-Reihe — die Divergenz der zwei Achsen als Satz. Bei `m = 3` sind Günthers zwei Formeln (S. 28) Anfang und Schritt | kein Satz trägt `Classical.choice`, gegen die Erwartung der Spec. Zwei Stellen hätten es gezogen: die Induktion über `Fintype (Pairs m)` (vermieden über `List.finRange`) und, nicht vorhergesehen, die Bit-Rechnung des Anfangs — `omega` auf einem Iff-Ziel mit Konjunktion, Fallstrick 7; zerlegt in atomare Ziele fiel der Choice aus Z, Z' und A8 heraus. Die „Hauptarbeit" A3 war über Schlüsselfunktionen ein einziges Lemma. Fallstrick 12 (zsh-Namensliste) beim Setzen der Wachen erneut aufgetreten und über Python umgangen |
| 2026-09-16 | `ffb8f09` | **Fallstrick 7, dritte Gestalt** — `omega` auf einem Iff-Ziel mit Konjunktion zieht `Classical.choice`; Beleg die zwei Profile von `base_bits` im Eintragstext, ohne Zeugensatz | der Anker `5896151` enthält nur die choice-freie Fassung; die andere stand allein in der Commit-Nachricht und ist am Stand `8fb050e` nachgemessen — beide Profile reproduziert. Eine eigene Formulierung zurückgenommen: der Choice sass vor der Heilung in Z und Z', A8 entstand erst danach. Keine Kennzahl bewegt sich |
| 2026-09-17 | `fd0eb83` | **Anspruchsverweis nachgeführt** — `ProemialCarrier` nennt die wechselseitige Bedingung jetzt A20-4: `Definitionen.md` §20 führt seit 17.9. sechs Ansprüche, A20-5/6/7 wurden A20-4/5/6 | die Umnummerierung trifft im Repo genau eine Zeile (`git grep` über alle verfolgten Dateien, Gegenprobe ohne Nummer); im Korpus steht die alte Zählung in rund 190 datierten Dokumenten und bleibt dort als Historie. Keine Kennzahl bewegt sich |
| 2026-09-17 | `8acd958` | **Z4 eingetragen** — `TowerAsymmetry` und `ProemialCarrier` nennen den Grund des Ausschlusses: die Stellenzahl hat ein Unten (`RGS 0`, aus der Typisierung), Günthers Ordnung nach §20 nicht; unter der zyklischen Lesart von [T14] wäre die Gleichsetzung unverträglich statt nur unbelegt | der Anlass ist der B4-Befund: der Bestand führt keine absteigende Induktion über eine proemiale Ordnung (0× `termination_by`, die vier starken Induktionen laufen über Listenpositionen und Iterationsränge), und das Unten des Modells kommt aus dem Trägertyp. Zwei Berichtigungen am Wortlaut der Entscheidung sind eingearbeitet: der Verweis auf eine §20-Marke, die mit der X4-Umformulierung gefallen war, und die Bedingung, die am Übergang S. 27 → Relationsordnungen sitzt statt an jener Marke. Keine Kennzahl bewegt sich |
| 2026-09-21 | `3a5ef78` | **`PatternInvariance` gebaut** — `pattern_characterization`: eine Menge von Tupeln ist genau dann unter allen Wert-Permutationen invariant, wenn sie mit jedem Tupel dessen ganze Musterklasse enthält. Das Lemma `orbit_of_same_pattern` konstruiert die Permutation explizit (`marksFull`, `viaLists` mit Umkehrung) statt sie über Endlichkeit zu erschliessen; der Kern `dedup_idxOf_congr` ist eine Induktion über die Muster-Form `Pat`. Erste Datei, die `Preserves` mit `canonicalize` in einem Satz verbindet — vierte Naht dieser Art. Löst A20-1 **nicht** ein (Ort ja, Stelligkeit zwei nein); keine Ledger-Zeile, kein §20-Träger. 17 Sätze, 17 Wachen, 0 Sorries. |
| 2026-09-21 | `f87a6a9` | **Quellenlage der drei Momente** — der Kopf von `ComplementaryMediation` führt jetzt, was die Quelle hergibt: die **Dreiheit** (reversibel, irreversibel, generelle Komplementarität) steht **einquellig** auf Lille S. 10, und `gg_struk-min-theor-obj-geist.pdf` ist dieselbe Quelle — kein zweiter Zeuge. Die **Zuordnung** Umtausch ↦ reversibel ist dagegen zweiinstanzlich (Lille S. 10 und `cat` S. 9, anderes Werk). Reine Prosa, Kennzahlen unbewegt. Gemeldet und nicht geheilt: die Zeilenangaben des Kopfes treffen die heutige Volltext-Datei nicht mehr (Dreiheit Z. 436–439 statt 484–508); die Seitenangabe stimmt. |
| 2026-09-21 | `5888989` | **Sieben Einträge in CLAUDE.md** — die drei Härte-Marken (`REFERAT` / `REFERAT, GEGENGERECHNET` / `GEMESSEN`) in §6 mit Rückweg nach §13.3; in §8 die zweite Gestalt von Fallstrick 12 (ungequotetes Here-Dokument führt Markdown-Backticks aus), **Fallstrick 21** (die Simp-Menge entscheidet, also die Importe — mit Reichweitenzahl 17 von 1011 und Messroute), **Fallstrick 22** (die nicht importierte Konstante verhält sich wie eine nicht existierende), ein Zusatz zu Nr. 10 (`Fin.fintype` trägt es auch ohne `deriving`), zwei Sätze an Nr. 2 (was `decide` sieht, entscheidet das Profil) und ein Vorspann, der die vier Abstände ordnet. Der §8-Titel ist geweitet — Fallstricke statt Lean-Fallstricke, weil Nr. 9, 12 und 22 keine Lean-Fälle sind. Reine Prosa, Kennzahlen unbewegt. |
| 2026-09-21 | `fecaa25` | **`wachen_namen.sh`** — erzeugt aus einer Lean-Datei die Messdatei für `#print axioms`, über dieselbe Satzroute wie `kennzahlen.sh`. Die Namen laufen durch `python3` statt durch die Wortteilung der Shell: das ist der ganze Zweck, denn Fallstrick 12 ist in diesem Strang zweimal aufgetreten. Nur die Namensliste, keine Profile. Geeicht an `PatternInvariance` — identisch mit der Handmessung vom selben Tag. |
| 2026-09-21 | `a7f9c10` | **`NegationCycle` gebaut** — Günthers „Hamiltonkreise" der Negatoren, Stufe 1 nach `KorpusRev2/Vorprobe_Hamiltonkreise_Impl.md`. Wertfolgen als Listen, der Negator als Werte-Umtausch, `IsFullCycle` entscheidbar; Tafel VI samt `triadic_unique` (dreiwertig nur ein Kreis, zwei Drehsinne), die drei Beispielkreise aus IGN 1979 S. 44–45 mit den Familien 10-9-5 / 9-6-9 / 6-12-6 (der dritte ist Tafel IV von 1980), Gegen-Drehsinn, Spiegelung, und Günthers Gegenbeispiel (23) mit 8-8-8. 22 Sätze, 22 Wachen, durchweg ohne `Classical.choice`. Nicht gebaut: die 44, Tafel XX, die Unmöglichkeit von 8-8-8. Drei Textbefunde im Kopf (zweiter Kreis mit 22 statt 24 Negatoren gedruckt; je eine Spalte in Tafel XII und IV). **Berichtigung in eigener Sache:** `docs/kennzahlen.md` trug seit `e01bf5a` 1407 NUL-Bytes und eine Statuszeile am Ende — ich hatte `./kennzahlen.sh --markdown` zusätzlich nach `docs/kennzahlen.md` umgeleitet, das Skript schreibt die Datei aber selbst. Ab diesem Eintrag wieder ohne Umleitung erzeugt, 77 Zeilen, NUL-frei. Die Werte waren in keiner Fassung falsch. |
| 2026-09-21 | `a334510` | **`NegationCycle`: die Emendation als Satz** — auf Anordnung des Architekten nach den Anmerkungen des Mathematikers. `kreis2_emendation`: unter allen Fortsetzungen der IGN S. 44 *gedruckten* Folge (22 Negatoren) um zwei Negatoren gibt genau `·1·3` einen Vollkreis — die Ergänzung ist erzwungen, nicht gewählt, und heisst im Kopf jetzt Emendation. `full_length`: ein Vollkreis über `m + 1` Werten hat `(m + 1)!` Negatoren (Günthers „n! Negationsschritte", 1980 S. 20). `triadic_unique'`: jeder dreiwertige Vollkreis ist (4) oder (5), ohne Längenvoraussetzung. +6 Sätze, +6 Wachen. **Das Modul ist nicht mehr durchweg choice-frei**: `full_length` und `triadic_unique'` tragen das volle Profil aus Mathlibs Sätzen über `List.permutations`; die übrigen 26 bleiben ohne `Classical.choice`. Im Kopf vermerkt: die Skala FOLGERUNG/HEBUNG/… ist auf diese Sätze nicht anwendbar (kein Bestandsverbrauch). |
| 2026-09-21 | `8e1baac` | **`NegationCycleTable`: mindestens vierundvierzig** — Stufe 2a nach der Spec des Mathematikers. 44 ausgeschriebene Negationsfolgen (drei Günther-eigen, 41 gerechnet; die Trennung steht in der Definition), jede ein Vollkreis, keine zwei gleich, **keine die Umkehrung einer anderen** — also 44 Kreise und nicht 44 Folgen über 22 Kreisen. Dazu die vier Verteilungen und: keine der 44 verteilt 8-8-8, **an diesen 44 und nicht als Unmöglichkeitssatz**. Dafür in `NegationCycle` der allgemeine `reverse_full`, den dessen Kopf als Lücke benannt hatte; die drei Beispiel-Umkehrsätze sind seither seine Folgerungen, ihr Profil steigt dabei von `[propext]` auf `[propext, Quot.sound]`. +17 Sätze, +17 Wachen, kein `Classical.choice`. **Zwei Befunde gegen die Spec:** „modulo Rotation und Drehsinn" quotiert zu weit — bei festem Ausgang gibt es keine Rotations-Freiheit, und wer doch quotiert, behält von den 44 noch sechs Bahnen; und die Entscheidung über die 88 gerichteten läuft in den `whnf`-Timeout, weshalb `gerichtet_full` bewiesen statt entschieden ist. |
| 2026-09-21 | `17f55b9` | **`NegationCycleSearch`: es gibt genau vierundvierzig** — Stufe 2b. `exactly_fortyfour`: eine Negationsfolge ist **genau dann** ein Vollkreis, wenn sie in der Tafel der 44 steht oder ihre Umkehrung es tut. Damit steht Günthers Zahl als Satz. Der Kern ist `search_complete` — Induktion über die Suchtiefe mit der Invariante „die Bitmaske kodiert die besuchten Stationen"; der Schritt ist zweimal ein Bit-Lemma über `ix_inj`. `alleB_perm_gerichtet` zeigt, dass Suche und Tafel dieselbe Liste sind, **bewiesen statt entschieden**. Die Bitmasken-Kodierung ist die Bedingung der Machbarkeit: dieselbe Suche mit Listen bricht im Kernel nach 3:42 bzw. 2:45 ab, mit der Maske läuft sie in 17,6 s — **nicht der Suchbaum ist zu gross, sondern die Kodierung des Zustands**. 19 Sätze, 19 Wachen. `search_complete` und seine Konsumenten tragen `Classical.choice` (Taktikwahl; die Heilung aus Fallstrick 21 greift dort nicht, gemessen) — benannt und nicht geheilt. |
| 2026-09-22 | `636f916` | **`NegationCycleSearch`: Günthers Zahl ohne `Classical.choice`** — auf Anordnung des Architekten, vom Mathematiker gebaut. `exactly_fortyfour` und die ganze Kette `search_complete` → `alleB_complete` → `alleB_perm_gerichtet` → `alleB_full` → `full_iff_mem` tragen jetzt `[propext, Quot.sound]`. **Es waren zwei Quellen verschiedener Gattung**, beide durch Konstanten-Analyse des lokal neu elaborierten Beweisterms benannt: in `search_complete` **Fallstrick 21** — offenes `simp` an den *Längen*-Zielen zog `Nat.add_eq_right._simp_1` und `Nat.right_eq_add._simp_1` aus der durch `Mathlib.Data.Nat.Bitwise` erweiterten Simp-Menge, geheilt an fünf Stellen durch `simp only … ; omega`; in `alleB_complete` **Fallstrick 10** — `NegationCycle.full_length` zieht Choice über `List.nodup_permutations` und `List.nodup_finRange`, geheilt durch `full_length3` für `m = 3` über das entschiedene `perms3_nodup` (axiomfrei). `full_length` bleibt in `NegationCycle` unverändert. **Die Zeile vom 21.9. (`17f55b9`) sagt, die Heilung aus Fallstrick 21 greife dort nicht — sie greift, an den Längen-Zielen; und sie war nicht die einzige Quelle.** +2 Sätze, +2 Wachen, sechs Wachen auf den gemessenen Wert gezogen. |
| 2026-09-22 | `23dabc1` | **`triadic_unique'` choice-frei** — Folgeposten aus der Abnahme der Choice-Heilung, auf Anordnung des Architekten. `perms2_nodup` (axiomfrei, `decide`) und `full_length2` ersetzen für drei Werte den Mathlib-Baustein, über den `full_length` das Choice einträgt (Fallstrick 10). Damit trägt in `NegationCycle` nur noch `full_length` Choice, zu Recht als der allgemeine Satz. Dasselbe Muster wie `full_length3`. +2 Sätze, +2 Wachen. |
| 2026-09-22 | `489984a` · `acdb3d5` | **Sammel-Vollzug 2, gegen grep-Beleg.** CLAUDE.md: `wachen_namen.sh` in §3 mit der Auflage „grüner Bau vor dem Messlauf"; an Fallstrick 21 die Schritte (4) Kette abwärts und (5) gegenfaktisch; neu Fallstrick 23 (gewacht ist nicht bewiesen — `decide` im Timeout ist axiomfrei) und 24 (`Finset` zahlt Choice nur über `Multiset.toFinset`). §8 von 22 auf 24 Einträge. Der Vorspann (F4), für den „0 Treffer" gemeldet war, stand schon, Z. 377 — ein Routenbefund. Ledger Rev. 21: L14-1 „bewusst nicht gebaut", Trägerstatus bleibt „Offen" (R4/R7); `ElementaryCycle`-Kopf angeglichen. Kein Satz, keine Wache. |
| 2026-09-22 | `612fd44` | **`janus1974` — die Emendation an Günthers eigenem Druck.** Günther druckt den zweiten Beispielkreis 1974 im *Janusgesicht der Dialektik* vollständig (Hermeneutes am Seitenbild). `janus1974_full`: ein Vollkreis; `janus1974_rotate`: genau `kreis2.rotate 23`, dieselbe Folge wie die emendierte, um einen Schritt rotiert. Beide `[propext]`. Im Kopf Custos' Wortlaut zur Quelle der Emendation und zur Druckvorlage (Hegeljahrbücher 1979, S. 22–88). +2 Sätze, +2 Wachen. |
| 2026-09-24 | `03de4b9` | **`decomp` — die Umkehrung der Wertzahl.** `intervalStart` geht von der Themenzahl zur Wertzahl, `decomp` zurück: jede Wertzahl zerfällt in Themenzahl und Überschuss, und nach `decomp_uniq` auf genau eine Weise. Vier Eichwerte (35/36, 65/66); die letzten zwei machen den 65/66-Befund vom 16. September zum Satz. +8 Sätze, +8 Wachen; Eichwerte und `decomp_succ` axiomfrei, der Rest `[propext, Quot.sound]`, kein Choice. Zwei gemessene Bau-Hinweise im Kopf: `decide +kernel` statt `decide` (Rekursionsgrenze bei m = 35), und **konjunktive Ziele vor `omega` zerlegen** — unzerlegt zieht derselbe Beweis `Classical.choice` (Fallstrick 7, vierte Gestalt). Der Kopf sagt, was der Satz nicht ist: kein AT-1b, kein Günther-Name, keine Ledger-Zeile. Aggregat-Beschreibung im selben Zug nachgeführt, samt des vorgemerkten Zähl-Fixes für `NegationCycle` (37 → 39). |
| 2026-09-24 | `d6f8429` | **`Exhausted` — Günthers Erschöpfung über `decomp`.** Der Überschuss hat die Themenzahl erreicht; `exhausted_seam` ist der `else`-Zweig von `decomp_succ` als Satz, `not_exhausted_step` sein Gegenstück. +2 Sätze, +2 Wachen; `[propext]` bzw. `[propext, Quot.sound]`, im Modul wie in der Probe. Eichungen als `example`. **Befund:** der Bestand trägt dasselbe Günther-Wort schon als `ExhaustionTransition.Exhausts` (Iteration zum Fixpunkt) — zwei Gestalten, Verhältnis offen. **Berichtigung in eigener Sache:** die Aggregat-Beschreibung von `IntervalBackbone` zählte seit `03de4b9` noch „drei decide-Sätze, fünf omega-Sätze"; jetzt nachgezählt, 18 Sätze in drei Klassen. |
| 2026-09-24 | `63c2fb4` | **`decomp_never_exhausts` — die zwei Gestalten der Erschöpfung schliessen einander aus.** In `ExhaustionTransition`: der Gang von `decomp` hat keinen Fixpunkt und fällt darum nie unter `Exhausts`; +3 Sätze, +3 Wachen, choice-frei, neue Importzeile ohne Jobkosten. In `IntervalBackbone` ist Lille S. 9 jetzt der wörtliche Hauptanker von `Exhausted` („Ein Intervall endet dort, wo …"); S. 19 gibt den Namen für den einen Fall `Exhausted 35`, und die Übertragung des Worts auf alle Nähte ist als unsere Wahl markiert. |
| 2026-09-24 | `ff8e357` … `4cb7695` | **Sammel-Vollzug 3, gegen grep-Zeile.** `janus1974_contains_printed` (+1 Satz, +1 Wache, `[propext]`); NegationCycle-Zählung im Aggregat 39 → 40; `ComplementaryMediation`: alle Lille-Anker von Zeile auf Seite und Wortlaut umgestellt (alle hatten gedriftet, nicht nur die zwei genannten); Ledger Grenznotiz C an L14-1 angeglichen; CLAUDE.md Fallstrick 25 (ein Choice-Profil beweist keine Nicht-Existenz) und an Nr. 7 eine Präzisierung statt einer vierten Gestalt. **§6 ohne Objekt:** Definitionen §3 führt keinen Indexwahl-Vermerk, dessen Verweis umzukehren wäre — gemeldet, `Definitionen.md` unberührt. |
| 2026-09-24 | `815fe68` | **Die Genese — Zopfrelation der Negatoren, HKN S. 25 als Eichung** (Spec V, Teil O3). In `NegationCycle`: `sw_val` (axiomfrei), `braid` für jedes `m` und `comm_far` (`[propext, Quot.sound]`), `braid_fails_far`, `genese_resultat`, `genese_verschieden` (`[propext]`); +6 Sätze, +6 Wachen, 40 → 46 im Modul. `N1.2.1` und `N2.1.2` geben denselben Umtausch auf verschiedenen Wegen; der Zopf gilt genau dort, wo zwei Negatoren einen Wert teilen. Kein §11-Träger, keine Definition der zweiten Negation, L07-4 offen; Teil O1 (Text) liegt bei Custos. |
| 2026-09-25 | `a5edb31` | **`braid_fails_far_all` — der Zopf scheitert für jedes entfernte Paar** (Folgezug nach der Abnahme von O3). Für jedes `m`, Zeuge stets der Wert `i`; `[propext, Quot.sound]`; +1 Satz, +1 Wache, 46 → 47 im Modul. Bis dahin stand das „genau“ in K4 nur an einem Beispiel; `braid_fails_far` ist jetzt dessen Eichung. Die Journalzeile kam einen Commit zu spät (Python-String brach am Anführungszeichen), die Kennzahlentafel am Anker `3732ac2` ist davon unberührt. |
| 2026-09-25 | `d61d85d` | **Der kalkültheoretische Bereich der zweiten Negation, bei drei Werten** (Spec V, O2). In `NegationCycle` Teil 6: Günthers vier Spalten aus HKN S. 24 (`transklassisch3`) sind genau das Komplement der Doppelstrich-Zone (`klassisch3`) in den sechs Anordnungen; Spalte für Spalte geeicht. +5 Sätze, +5 Wachen, 47 → 52 im Modul, alle `[propext]` — auf `Perm` trug derselbe Inhalt Choice. Löst V nicht ein; kein `m ≥ 4`, keine Definition der zweiten Negation, L07-4 offen. |
| 2026-09-25 | `77e5a1a` | **`NegationCycleSymmetry` — Günthers Familienrede auf Sätzen** (Zug 1). `no_888_all`: Günthers „niemals“ 8-8-8 (IGN S. 43) für jeden Vollkreis; `mirror_all`: die Spiegelung `N₁ ↔ N₃` für jeden Vollkreis bei vier Werten; `mirror_gerichtet` als Eichung. +1 Modul, +3 Sätze, +3 Wachen, kein `Classical.choice`. **Zehn Folgestellen berichtigt** in vier Dateien: neun nach der Spec (gemeldet waren drei), darunter die falsche Zeile aus `d61d85d` („für m ≥ 4 sagt die Quelle nichts“), und eine zehnte im Aggregat: `NegationCycleSearch` stand dort seit `636f916` noch als „nicht geheilt“. |
| 2026-09-25 | `87bf5d3` | **Der kalkültheoretische Bereich für jede Wertzahl** (Zug 2, `NegationCycle` Teil 7). `transklassisch_card`: bei `m + 2` Werten `(m + 2)! − 2` Elemente — Definitionen §7 als Satz, die Zahl zu Günthers „erweitert sich … ganz enorm“ (HKN S. 24); `Classical.choice` aus `nodup_finRange` und `nodup_permutations` wie `full_length`. Brücke zu Teil 6 (`transklassisch_eq_three`), Eichungen bei vier (22) und zwei Werten (leer), choice-frei. +8 Sätze, +8 Wachen, 52 → 60 im Modul. Löst V nicht ein. |
| 2026-09-25 | `c27329d` | **`NegationCycleGenerators` — das Negationssystem ist der Inbegriff aller Permutationen** (Zug 3, Teil A; tml S. 10). Brücke `sw_eq_swap` / `negPerm_eq_swap` zu Mathlibs Nachbartransposition; `negators_generate_all`: die Negatoren erzeugen jede Permutation, für jedes `m` — „gemessen bis m = 7“ ist jetzt ein Satz (Buchung in Definitionen §7 bei Custos). `Classical.choice` aus `Equiv.swap` und dem Submonoid-Abschluss, nicht heilbar. +1 Modul, +3 Sätze, +3 Wachen; Build-Jobs 1345 → 1414, weil `Perm.Sign` in die Aggregathülle tritt. |
| 2026-09-25 | `4ae0914` | **`NegationCycleSJT` — die Steinhaus–Johnson–Trotter-Konstruktion der Vollkreise** (SJT-Zug, Teil A). Vollkreis bei zwei, drei und vier Werten; bei einem Wert keiner (`no_full_at_one_value`); `reach_of_full`: aus einem Vollkreis folgt die Listen-Fassung von N-a, konstruktiv. Eichung: `sjt2_eq` = Günthers Folge (5), `sjt3_mirror_kreis2` = der gespiegelte emendierte zweite Kreis, ein Zeuge der Emendation von außerhalb des Textes. +1 Modul, +8 Sätze, +8 Wachen, alle `[propext]`; Build-Jobs +1. Teil B (jede Wertzahl) nicht gebaut. |
| 2026-09-25 | `aa0332c` | **Der vermittelnde Wert — `track_mediates`** (`NegationCycle` Teil 5). Unter jedem Negatorwort durchläuft ein Wert jeden Wert zwischen Anfang und Ende, in beide Richtungen, für jedes `m` — Günthers „der Wert 2 spielt eine vermittelnde Rolle zwischen 1 und 3“ (HKN S. 25) in der Lesart „vermittelnd = durchlaufen“, markiert; zwei andere Lesarten genannt. +5 Sätze, +5 Wachen, 60 → 65 im Modul, kein `Classical.choice`, Build-Jobs unverändert. §11-Buchung bei Custos. |
| 2026-09-25 | `6545ebd` | **Ein Vollkreis für jede Wertzahl ab zwei** (SJT-Zug, Teil B; `NegationCycleSJT` Teil 2). `sjt_full`: die Steinhaus–Johnson–Trotter-Folge ist für jede Wertzahl ab zwei ein Vollkreis; daraus `full_exists` — Günthers stille Voraussetzung, mit `no_full_at_one_value` genau ab zwei Werten — und `reach_all`, die Listen-Fassung von N-a für jedes `m`, konstruktiv und choice-frei. Der Kern `stations_blocks`: die Stationen des großen Kreises sind die Einfügungen in die des kleinen. +45 Sätze, +45 Wachen (53 im Modul), kein `Classical.choice`, Build-Jobs unverändert. |
| 2026-09-25 | `33c68bd` … `a6ef7c8` | **Sammel-Vollzug 4: die V-Nachführungen, gegen gemessene Prämissen.** Ledger: L07-4 mit zwei datierten Gründen (Hegel-Anspruch 26.7., „Menge“ 14.–16.9.), neue Zeile L11-6 `NCyc.track_mediates` mit eigenem Anker HKN S. 25, Grenznotiz C mit Verweis auf `sjt_full`; Selbstauskunft 112/98/90/74. CLAUDE.md: Fallstrick 10, die Nodup-Familie. `Definitionen.md` §7 (außerhalb des Repos, Hash `393abaea…` → `a5f8391d…`): W1, W2, C2, C2′. **Gemeldet:** W2 überschneidet sich mit dem schon vorhandenen Tafel-II-Absatz; Z. 328 trug neben N-a auch N-b, das kein Satz ist; SharedPlaceGrowth hat keine Ledger-Zeile für den Rückverweis. |
| 2026-09-25 | `ffac1df` | **K4 geschärft, W2 nachgeführt.** `NegationCycleSJT`-Kopf: „die Konstruktion, die Günther nicht hatte“; die Aussage „trifft bei drei und vier Werten seine eigenen Kreise“ vor dem Schreiben gemessen — drei Werte `sjt2_eq`, vier Werte `sjt3_mirror_kreis2` (gespiegelt). Die Rückfallfassung des Prompts („bei drei Werten seinen emendierten zweiten Kreis“) wäre falsch gewesen: `sjt 3` läuft über vier Werte. `Definitionen.md` §7 (außerhalb des Repos, `a5f8391d…` → `7f0f4847…`): Tafel-II-Absatz ohne doppeltes Zitat, W2 mit dem Halbsatz zu den drei Lesarten. |
| 2026-09-25 | `39ca8cb` … `98ff22b` | **Sammel-Vollzug 5.** Ledger Rev. 21 → 22 (entschieden mit L11-6, bis dahin nicht vollzogen; die Zahl stand nur im Kopf). CLAUDE.md: die Ertrags-Skala (fünf Stufen, mit Route) als Unterabschnitt von §4, Route-Schritt (2) als VORGESCHLAGEN markiert; kein neuer nummerierter Abschnitt, damit „§8“/„§12“-Verweise nicht wandern. `Definitionen.md` §7 (außerhalb des Repos, `7f0f4847…` → `73900af0…`): die dritte Lesart „dem Wort nach“. |
| 2026-09-25 | `80d875d` | **Metamorphose der Zahl als zweiter Anker der Genese.** `NegationCycle` Teil 5: K1 nennt Günthers Primat-Frage (PDF-S. 9, gedr. 8), K-V1 seine „Vermittlung“ (PDF-S. 9/8, gedr. 8/7). Der beauftragte Satz `genese_kreis` wurde **nicht** gebaut: er wäre eine Dublette von `tafelVI4_full` (`[0,1,0] ++ [1,0,1] = tafelVI4` definitionsgleich); die Zerlegung steht als `example`. Tafel IIIa ist Günthers Folge (4). Sätze und Wachen unverändert. |
| 2026-09-25 | `14b9925` | **`TimeMeasure` — das gegenständliche Zeitmaß.** Günthers WWZ S. 3 („kein gemeinsames, objektives (gegenständliches) Zeitmaß“) als Satz in der Lesart der Gegenläufigkeit: zwei Zeiten, die einander umkehren, haben jede ihr Maß und kein gemeinsames, für jeden Träger. Das Maß über ℤ, nicht ℕ (ein ℕ-Rang setzte ein Unten voraus). Brücke Maß ⇒ Ordnung. +1 Modul, +5 Sätze, +5 Wachen, kein `Classical.choice`. Nicht gebaut: die Wahl selbst (WWZ S. 6). |
| 2026-09-25 | `eaca6eb` | **`TimeMeasure`-Kopf nachgeführt.** Nach der Abnahme: `measure_noreturn` ist eine HEBUNG (ℤ statt ℕ), keine Folgerung. Nach Hermeneutes (WWZ S. 3 am Seitenbild): dass jede Zeit ihr Maß hat, ist quellenfest („jene beiden Zeitmaße“), die Reversible-Lesart fällt am Wortlaut, die Umkehrung ist quellenfest als Rangumkehr, „gegenläufig“ als Laufrichtung bleibt Lesart. Sätze unverändert. |
| 2026-09-25 | `819de20` … `f6a69ae` | **Sammel-Vollzug 6.** `TimeMeasure`: `reversible_no_measureZ` als INSTANZIIERUNG statt „Korollar“ (keine Skalenstufe); die V6-Stelle stand nicht im Kopf. **Lille-Zeilensweep:** alle Lille-Zeilenanker in neun Dateien auf Seite und Wortlaut, jede Stelle am heutigen Volltext nach Wortlaut gesucht — gemeldet waren zwei Stellen, es waren über fünfzig Vorkommen; keine alte Zeile traf mehr. |
| 2026-09-25 | `1e5021c` | **Mathlib-Anker nach Deklarationsnamen (Custos U3).** In `AlphaGammaSubstantial` tragen drei Mathlib-Verweise statt der Zeile jetzt den Namen, gemessen per `#check`: `Functor.IsEquivalence` (vorher „Zeile 610“), die anonyme Instanz `Adjunction.instIsIsoFunctorCounitOfIsEquivalence` (vorher „Z. 255“, der Name ist vom Elaborator erzeugt) und `NatIso.isIso_app_of_isIso` (vorher „Z. 165“). Weitere Mathlib-Zeilenanker in `MathlibExtensions`, `Kenogram`, `Proemial` und `PathC` sind gemeldet, nicht geändert. |
| 2026-09-25 | `50fa4f0` | **Sammel-Vollzug 7: die übrigen Mathlib-Anker.** Elf Zeilenanker tragen jetzt den Deklarationsnamen, jeder per `#check` bestätigt: die acht der U3-Fundliste und drei, die deren Route nicht fand. Zwei davon sind anonyme Instanzen mit vom Elaborator erzeugtem Namen, das ist vermerkt. Die sechs Verweise auf das Mathlib-TODO „Show that every topos is regular“ tragen Modul, Wortlaut und Stand (Mathlib `83a5988`, 2026-05-05); das TODO steht an dieser Revision noch da. Außerhalb von `PathC` hängt kein Mathlib-Anker mehr an einer Zeile; in `PathC` (eingefroren) bleiben sechs. |
| 2026-09-25 | `b70eeb5`, `1b6114f` | **PB45 und GEN.** PB45: Das Fehlen, auf das `PullbackLemmas` sich stützt (ein rechter Adjungierter zu `Subobject.pullback f` im Topos), ist an Mathlib `83a5988` gemessen und besteht: auf Subobjekt-Ebene nur ∃_f ⊣ pullback_f, auf Over-Ebene nur die Hypothesenklasse `ExponentiableMorphism` ohne Instanz für einen Topos. Der Anker zeigt jetzt auf diese Messung statt auf das Regularitäts-TODO. GEN: `MathlibNameGuard` ist ein eigenes Default-Target und nennt die drei vom Elaborator erzeugten Instanznamen; ein verfälschter Name macht den Bau rot. Module 200 → 201, Build-Jobs 1416 → 1592. |
| 2026-09-25 | `9c3a7b0`, `cc6c3e5` | **Folgezug PB45 und GEN.** In `PullbackLemmas` steht die Messung des Fehlens an einem Ort, dem Doc-String der Klasse `PullbackISup`, mit `ExponentiableMorphism` als nur annehmbarer Klasse und der Einordnung „Mathlib-Lücke, keine Zusatzannahme“ (Standard der Topostheorie, im Bestand nicht bewiesen). Modulkopf und Hypothesenliste verweisen darauf. `MathlibNameGuard` hat den Default-Bau verlassen und wird beim Mathlib-Bump gefahren; die Wache hält jetzt dreizehn Namen. Ein verfälschter Name macht sie rot, den Default-Bau nicht. Eine Bump-Checkliste gibt es im Repo nicht. Build-Jobs 1592 → 1416. |
