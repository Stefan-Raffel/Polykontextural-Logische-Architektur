-- F3.a — Endofunctor construction with stage-modulated structure and
-- the four constituents (Endofunctor, Valuation, SchemaMorphism,
-- DoubleValuation) of the F3.a aggregate.
--
-- Reformulation-Verweis: Klärungs-Sitzungen 1, 2, 3 (F3a_Klaerung_*.md)
-- und F3a_Spec.md. Strukturmerkmale:
--
-- * Endofunctor F as composition of three functors — DesignativeRestriction,
--   OuterBalance (colimit), Skeleton — with three existence theorems
--   (EA1, EA2, EA3) and one non-existence theorem (NEA1, syntactic form);
-- * 2-categorical valuation form with subtopos choice family
--   (Bicategory structure deferred to future revision);
-- * Schema (V, E, S) data structure with schema morphisms preserving
--   the Modus-3 character of self-reference markers;
-- * Double valuation structure for hybrid two-layer belegungs of
--   the invariant layer (D2 / T11 IV);
-- * Stage-modulated form: Stage = ℕ, with ω sub-differentiation
--   (initializing / continuing) reflecting the B5/B6 distinction.

import Reformulation.F3a.Stage
import Reformulation.F3a.SchemaMorphism
import Reformulation.F3a.Endofunctor
import Reformulation.F3a.Valuation
import Reformulation.F3a.DoubleValuation
